---
'solana-mobile': patch
---

Fix `package.json` `bugs`, `homepage`, and `repository` URLs to point to `solana-mobile/solana-mobile-cli` instead of a stale `beeman/solana-mobile` reference, and correct the `license` field to `Apache-2.0` to match the `LICENSE` file.
